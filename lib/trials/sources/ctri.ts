import { TrialRecord } from "../types";

// Placeholder: CTRI does not provide a simple open API. Return empty list for now.
export async function fetchCTRI(_: any): Promise<TrialRecord[]> {
  return [];
}
